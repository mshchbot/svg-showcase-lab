# Workflow disabled — Pages deploys from gh-pages branch.
# This file exists only to prevent GitHub's default workflow from being re-created.
# To re-enable Actions-based deployment, switch Pages source to "GitHub Actions" first.

name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:  # manual only — won't run on push

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages deploys from gh-pages branch. No action needed."